[project]
name = "rex-desktop-worker"
version = "0.1.0"
description = "Foreground Windows desktop capture and computer-use MCP worker for Rex Desktop Bridge"
requires-python = ">=3.11,<3.15"
dependencies = [
  "mcp==1.29.0",
  "mss==10.2.0",
  "pywinauto==0.6.9",
]

[tool.uv]
package = false
